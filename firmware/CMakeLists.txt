# ESP-IDF project wrapper. PlatformIO adds src/ as the main component and
# auto-discovers components/ (robottwin_core wraps the repository's src/).
cmake_minimum_required(VERSION 3.16.0)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(rt-arm-fw)
